{
  "name": "live-moodboard",
  "version": "1.0.0",
  "description": "Place URL reference cards on canvas and preview sites live in a panel",
  "main": "code.js",
  "scripts": {
    "build": "node node_modules/typescript/bin/tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "^5.3.2"
  }
}
